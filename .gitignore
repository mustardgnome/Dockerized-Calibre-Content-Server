# --- Calibre libraries ---
# Ignore everything inside the library folders
calibre-libraries/**

# But DO NOT ignore the top-level folder itself
!calibre-libraries/

# But DO NOT ignore the subfolders (so they show up)
!calibre-libraries/Books Library/
!calibre-libraries/Manga Library/

# But ignore everything inside those subfolders except .gitkeep
calibre-libraries/Books Library/**
calibre-libraries/Manga Library/**

# Allow .gitkeep files
!calibre-libraries/Books Library/.gitkeep
!calibre-libraries/Manga Library/.gitkeep

# --- Calibre config ---
calibre-config/**
!calibre-config/
!calibre-config/.gitkeep

.DS_Store
**/.DS_Store
